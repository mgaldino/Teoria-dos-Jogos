\usepackage{tcolorbox}

\newtcolorbox{blackbox}{
  colback=gray, % Background color of the box
  colframe=black, % Frame color
  coltext=white, % Text color
  boxsep=5pt, % Inner separation between text and frame of the box
  arc=4pt % Corner arc radius
}
